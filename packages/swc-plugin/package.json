{
  "name": "@cassida/swc-plugin",
  "version": "0.7.0",
  "private": true,
  "description": "Cassida SWC plugin (WASM). Detects cas() chains in JSX spreads and emits an Op[] IR for the @cassida/next-plugin loader to compile against @cassida/compiler. Phase 1 scaffold — no-op transform. Private until the transform is filled in.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pishio/cassida.git",
    "directory": "packages/swc-plugin"
  },
  "main": "./dist/cassida_swc_plugin.wasm",
  "exports": {
    ".": "./dist/cassida_swc_plugin.wasm",
    "./loader": {
      "types": "./loader.d.ts",
      "import": "./loader.mjs",
      "require": "./loader.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/cassida_swc_plugin.wasm",
    "loader.cjs",
    "loader.mjs",
    "loader.d.ts",
    "README.md"
  ],
  "scripts": {
    "build": "cargo build --release --target wasm32-wasip1 && mkdir -p dist && cp ../../target/wasm32-wasip1/release/cassida_swc_plugin.wasm dist/",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "fmt": "cargo fmt --check",
    "prepublishOnly": "pnpm build"
  },
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  }
}
