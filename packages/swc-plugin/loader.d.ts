/**
 * Absolute filesystem path to the bundled SWC plugin WASM artefact.
 * Pass this string to whatever expects a plugin path — Next.js's
 * `experimental.swcPlugins`, the SWC CLI's `--plugin` flag, etc.
 */
export declare const wasmPath: string;
